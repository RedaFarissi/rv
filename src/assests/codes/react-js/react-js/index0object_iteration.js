const index0object_iteration =()=>{
  return`const person = { name: 'Sara', age: 30 };
    for (let key in person) {
    console.log(\`\${key}: \${person[key]}\`);
}`
}
export default index0object_iteration