const index6props_children0=()=>{
  return`import React from 'react';

const Card = (props) => {
  return (
    <div className="card">
      {props.children}
    </div>
  );
};

export default Card;`
}

export default index6props_children0